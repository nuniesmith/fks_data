services:
  fks_data:
    build:
      context: .
      dockerfile: shared/shared_docker/templates/Dockerfile.python
      args:
        SERVICE_NAME: fks-data
        SERVICE_TYPE: data
        SERVICE_PORT: 4200
        PYTHON_VERSION: "3.11"
    working_dir: /app/src
    command: ["python", "app.py"]
    environment:
      TZ: America/Toronto
      FKS_DATA_HOST: 0.0.0.0
      FKS_DATA_PORT: 4200
      PYTHONPATH: /app
      FKS_SERVICE_NAME: fks-data
      FKS_SERVICE_TYPE: data
      FKS_SERVICE_PORT: 4200
    ports:
      - "4200:4200"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - data_volume:/var/lib/fks/data
    networks: [fks_net]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:4200/health || python -c 'import sqlite3,os; sqlite3.connect(\"/var/lib/fks/data/active_assets.db\").close(); print(1)' || exit 1"]
      interval: 60s
      timeout: 8s
      retries: 2
      start_period: 25s

networks:
  fks_net:
    name: fks_net
    external: true

volumes:
  data_volume: